#include<bits/stdc++.h>
using namespace std;
int main()
{
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif
	int T;
	cin >> T;
	while (T--)
	{
		int N, Q;
		int marks[N], rank[Q];
		for (i = 0; i < 2 * n; i + 2)
		{
			cin >> marks[i] >> marks[i + 1];
		}
		for (i = 0; i < Q; i++)
			cin >> rank[i];



	}
}
return 0;
}